import { Character, Episode, Episodes, CharacterLocation } from "./types";
import { setHeaderContainer } from "./setHTML.js";
import {  } from "./promiseFunction";


window.addEventListener(
    "load",
    setHeaderContainer,
    
    
    );
    